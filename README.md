# Context-aware mock api generator

## Description
An LLM-powered app that generates realistic, context-aware mock API responses.

## Handy dev scripts

`docker compose up --build`
`./.venv/Scripts/ruff check . --fix`
`./.venv/Scripts/ruff format .`
`./.venv/Scripts/pytest tests/api/<filename>.py`
